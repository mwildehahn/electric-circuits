# GOV-002 execution handoff — attempt 1

Status: ready_for_review (prepared patch only; no commit or push).

Task: GOV-002
Execution scope: shared-d39415c0e97552c0393f1361a7703a27c7c0dfc3af1578e88337f4bbfdb610a7
Attempt: a1
Packet SHA-256: `0c53a85035b0cc4be0c1d2e6110cfe954e03e3ad12c8187b8e78b9087e1ebcaf`
Lease: `59cfba51-b1c1-4e1c-a4ff-f2bd07764a24`
Scheduler generation: 16

## Bound source and ownership

- Author worktree: `/tmp/electric-circuits-worker-gov-002-g16`
- Evidence run root: `/tmp/electric-circuits-gov-002-run-uv7Deb` (controller-provided; no output was written there)
- Pinned base/integration commit: `a0e733be36c2855494fe898b43dd63d494d14566`
- Pinned integration tree: `e0c7af27d4d333b3bf1df4dff9b1ff4a7e505d62`
- Allowed repository paths only:
  - `docs/production/support-capacity-manifest.json`
  - this execution handoff
- No commit or push was authorized or performed.

## Implementation

Added a machine-readable support and capacity manifest. It selects a narrow first-release target:
PostgreSQL 18.x with SCRAM and verify-full TLS, one writable primary, one active engine per logical
slot, one file-backed durable-streams instance, and one authenticated gateway. It enumerates supported
and typed-unsupported query, type, schema, platform, topology, and recovery behavior. The workload
section gives nonzero offered and accepted rates and distributions for writes, transactions, clients,
feeds, long polls, renewals, shapes, rows, width, groups, fan-out, selectivity, tenant skew, churn,
dormancy, and slow readers. Resource budgets cover CPU, RAM, disk, IOPS, FDs, connections, and reserve
headroom for engine, durable streams, gateway, PostgreSQL, and client nodes. Queue bounds and overload
actions, rejection/error isolation budgets, RPO/RTO/refetch objectives, fixed seeds/counts, warm-up and
sampling cadence, estimators, confidence/variance limits, retained-data normalization, and explicit
unknowns with owning tasks are recorded. Values are targets, not observed qualification evidence.

## Artifact identity

```text
docs/production/support-capacity-manifest.json
  sha256: b07f204770476b46406c1ae4db163381b686ad25d092f414fff974ca218ba91b
  git blob: 99e15986aa5e2daf72d4acaf22e18e22253156ad
prepared artifact-only patch sha256: e6c63daeadb228e954cf447ab7bb9187bb652171f62a2d8eb80a46e870340adf
```

## Gates and evidence

1. `git diff --no-index --check /dev/null docs/production/support-capacity-manifest.json` — PASS; no whitespace diagnostics.
2. `python3 -m json.tool docs/production/support-capacity-manifest.json` — PASS; manifest parses as JSON.
3. `pnpm typecheck` — PASS (exit 0) from the clean integration checkout `/Users/bozilabs/labs/electric-circuits`.
4. `pnpm typecheck` — BLOCKED in the detached author worktree because the declared read-only dependency mount was absent; a temporary symlink to the control checkout's `node_modules` still left workspace package links unresolved (`@electric-circuits/*`, `@trpc/server`, `zod`, `pg`, and related modules). The symlink was removed after the run; no source mutation remains.

The repository typecheck result is an inherited control characterization; the JSON artifact has no
TypeScript surface. The direct whitespace and parse checks are task-local evidence.

## Risks and follow-up

- All numeric values are explicitly marked target/unqualified and must be replaced or confirmed by the signed CAP-003A/CAP-003Q capacity operating-point manifest.
- Provider restore-frontier, filesystem fsync, and client-device RSS remain typed unknowns owned by OPS-002, CAP-003A, and CAP-005.
- Unsupported features intentionally include seamless failover, active-active/multi-region, CDN, browser clients, changes-only/SSE/order/offset, native subset, transaction-atomic observers, and virtual generated columns.
- The controller/integration operator must compute the final two-artifact patch identity and independently rerun direct gates from a fresh source.
